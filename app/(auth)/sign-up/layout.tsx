import { Metadata } from "next";

export const metadata: Metadata = {
  title: "SyncHub - Sign-Up",
};

export default function SignUpLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
